---
title: Types
description: Complete grouped reference for the public TypeScript types exported by the Pedelec SDK.
sidebar:
  order: 4
---

All types below are exported from `@kaoruisaac/pedelec`.

```ts
import type {
  PedelecOptions,
  ProviderCode,
  ProviderInfo,
  PedelecSessionStatus,
  ToolArgsSchema,
  SandboxAsset,
  SandboxAssetPath,
  ReadAssetType,
  CreateSessionSandboxInput,
  SandboxFolderPickerResult,
} from "@kaoruisaac/pedelec";
```

## Client and session input

### `PedelecOptions`

```ts
type PedelecOptions = {
  bridgeTimeoutMs?: number;
};
```

### `ProviderCode`

```ts
type ProviderCode =
  | "codex"
  | "antigravity"
  | "opencode"
  | "cursor"
  | "claude"
  | "ollama";
```

### `CreateSessionInput`

```ts
type CreateSessionInput<
  TTools extends readonly ToolDefinition[] = readonly ToolDefinition[],
> =
  | {
      provider: ProviderCode;
      effortLevel?: "default" | "low" | "high";
      skills?: SkillsInput<TTools>;
      sandbox?: CreateSessionSandboxInput;
      autoEndOnDisconnect?: boolean;
    }
  | {
      provider?: undefined;
      effortLevel?: "default" | "low" | "high";
      skills?: SkillsInput<TTools>;
      sandbox?: CreateSessionSandboxInput;
      autoEndOnDisconnect?: boolean;
    };
```

`effortLevel` is provider-independent and may be used with or without `provider`.

### `CreateSessionSandboxInput`

```ts
type CreateSessionSandboxInput = {
  path: string;
};
```

`path` is an absolute application-managed workspace path. Pedelec creates missing sandbox subdirectories but never deletes this workspace. It may be shared by multiple active sessions and must not overlap the managed sandbox root.

### `SandboxFolderPickerResult`

```ts
interface SandboxFolderPickerResult {
  path: string;
  isEmptyFolder: boolean;
  hasSandboxConfig: boolean;
}
```

This is the read-only snapshot returned by `sandboxFolderPicker()`. `hasSandboxConfig` only indicates that `.pedelec-lock.json` is a regular file; it does not validate the marker contents.

## Provider and settings

### `ProviderInfo`

```ts
type ProviderInfo = {
  name: string;
  code: ProviderCode;
  available: boolean;
  error: string | null;
};
```

### `PedelecSettings`

```ts
type PedelecSettings = {
  defaultProvider: ProviderCode | null;
};
```

### `ApprovalStatus`

```ts
type ApprovalStatus = {
  installed: boolean;
  approved: boolean;
  origin: string | null;
  appConnected: boolean;
};
```

### `PedelecAvailability`

```ts
type PedelecAvailability = {
  available: boolean;
  extension: { available: boolean };
  approval: { approved: boolean; origin: string | null };
  desktop: { available: boolean; launchAttempted: boolean };
  error?: PedelecError;
};
```

`PedelecSettings` is a public defaults-only DTO: it never contains provider credentials. `ProviderInfo` only exposes `name`, `code`, `available`, and `error`.

`available` requires every layer. `launchAttempted` can be true once the non-sensitive approval-status ping is sent; it does not confirm a process launch. `appConnected` reflects that ping only, not approval or provider readiness.

## Assets

### `SandboxAssetPath`

```ts
type SandboxAssetPath = `/${string}`;
```

The SDK public path for an asset in the session's shared store. Physically, the Agent-visible shared store is `<sandbox.path>/.pedelec-sandbox/assets/`; the `.pedelec-sandbox` directory is private runtime layout and is not exposed in SDK values. For example, `/images/photo.png` refers to `<sandbox.path>/.pedelec-sandbox/assets/images/photo.png`. Runtime validation also rejects backslashes, empty path segments, `.` and `..`.

### `ReadAssetType`

```ts
type ReadAssetType = "text" | "json" | "file";
```

Selects the conversion performed by `PedelecSession.readAsset()`: strict UTF-8 text, parsed JSON, or a browser `File`.

### `SandboxAsset`

```ts
type SandboxAsset = {
  name: string;
  path: SandboxAssetPath;
  sizeBytes: number;
  modifiedAt: number;
};
```

`modifiedAt` is the sandbox filesystem modification time. `path` is sandbox-relative and does not reveal the browser user's absolute local path.

## Errors and status

### `PedelecError`

```ts
type PedelecError = {
  code: string;
  message: string;
  details?: unknown;
};
```

Do not assume `details` has one global shape. Narrow by `code` and validate before reading.

### `PedelecSessionStatus`

```ts
type PedelecSessionStatus =
  | "idle"
  | "running"
  | "waiting_tool_result"
  | "ended"
  | "error";
```

## JSON value types

### `JsonPrimitive`

```ts
type JsonPrimitive = string | number | boolean | null;
```

### `JsonValue`

```ts
type JsonValue =
  | JsonPrimitive
  | JsonValue[]
  | { [key: string]: JsonValue };
```

Used by tool schema metadata. Runtime tool results should follow the same practical JSON-compatible constraints even when their TypeScript return type is broader.

## Tool definitions

### `ToolSpecificHandler`

```ts
type ToolSpecificHandler<TArgs = unknown, TResult = unknown> = (
  args: TArgs,
  ctx: ToolCallContext,
) => TResult | Promise<TResult>;
```

### `ToolDefinition`

```ts
type ToolDefinition<
  TArgs = unknown,
  TResult = unknown,
  TName extends string = string,
> = {
  name: TName;
  description: string;
  argsSchema: ToolArgsSchema;
  timeoutMs?: number;
  handler?: ToolSpecificHandler<TArgs, TResult>;
};
```

### `SkillsInput`

```ts
type SkillsInput<
  TTools extends readonly ToolDefinition[] = readonly ToolDefinition[],
> = {
  guidance: string;
  tools: TTools;
};
```

### `ToolNameOf`

```ts
type ToolNameOf<TTools extends readonly ToolDefinition[]> = Extract<
  TTools[number]["name"],
  string
>;
```

### Serializable manifests

```ts
type SerializableToolManifest = {
  name: string;
  description: string;
  argsSchema: ToolArgsSchema;
  timeoutMs?: number;
};

type SerializableSkillsManifest = {
  guidance: string;
  tools: SerializableToolManifest[];
};
```

Inline handlers are not part of the serialized manifest.

## Tool schema metadata

```ts
type ToolArgsSchemaMeta<
  TDefault extends JsonValue = JsonValue,
> = {
  description?: string;
  default?: TDefault;
  examples?: TDefault[];
};
```

## Tool schema nodes

### String

```ts
type ToolArgsStringSchema = ToolArgsSchemaMeta<string> & {
  type: "string";
  enum?: string[];
  minLength?: number;
  maxLength?: number;
  pattern?: string;
};
```

### Number

```ts
type ToolArgsNumberSchema = ToolArgsSchemaMeta<number> & {
  type: "number";
  enum?: number[];
  minimum?: number;
  maximum?: number;
};
```

### Integer

```ts
type ToolArgsIntegerSchema = ToolArgsSchemaMeta<number> & {
  type: "integer";
  enum?: number[];
  minimum?: number;
  maximum?: number;
};
```

### Boolean

```ts
type ToolArgsBooleanSchema = ToolArgsSchemaMeta<boolean> & {
  type: "boolean";
  enum?: boolean[];
};
```

### Array

```ts
type ToolArgsArraySchema = ToolArgsSchemaMeta<JsonValue[]> & {
  type: "array";
  items: ToolArgsSchemaNode;
  minItems?: number;
  maxItems?: number;
  uniqueItems?: boolean;
};
```

### Object

```ts
type ToolArgsObjectSchema = ToolArgsSchemaMeta<
  Record<string, JsonValue>
> & {
  type: "object";
  properties?: Record<string, ToolArgsSchemaNode>;
  required?: string[];
};
```

### One-of

```ts
type ToolArgsOneOfSchema = ToolArgsSchemaMeta & {
  oneOf: ToolArgsSchemaNode[];
};
```

### Node and root aliases

```ts
type ToolArgsSchemaNode =
  | ToolArgsStringSchema
  | ToolArgsNumberSchema
  | ToolArgsIntegerSchema
  | ToolArgsBooleanSchema
  | ToolArgsArraySchema
  | ToolArgsObjectSchema
  | ToolArgsOneOfSchema;

type ToolArgsSchema = ToolArgsObjectSchema;
```

The root alias enforces an object schema at compile time.

## Event contexts

### `PedelecEventContext`

```ts
type PedelecEventContext = {
  sessionId: string;
  provider: string;
  effortLevel?: "default" | "low" | "high";
  sessionCreatedAt: number;
  eventReceivedAt?: number;
  eventEmittedAt: number;
  turnId?: string;
  turnStartedAt?: number;
  turnKind?: "user" | "prepare";
  source: "core" | "sdk";
};
```

### `ChatEventContext`

```ts
type ChatEventContext = PedelecEventContext & {
  type: "chat_delta";
  turnId: string;
  turnStartedAt: number;
  eventReceivedAt: number;
};
```

### `ToolCallContext`

```ts
type ToolCallContext = PedelecEventContext & {
  type: "tool_call";
  toolRequestId: string;
  tool: string;
  turnId: string;
  turnStartedAt: number;
  eventReceivedAt: number;
};
```

### `StatusEventContext`

```ts
type StatusEventContext = PedelecEventContext & {
  type: "status_changed" | "sdk_status_changed";
  status: PedelecSessionStatus;
  previousStatus: PedelecSessionStatus;
};
```

### `ErrorEventContext`

```ts
type ErrorEventContext = PedelecEventContext & {
  type: "error" | "sdk_error";
};
```

### `EndedEventContext`

```ts
type EndedEventContext = PedelecEventContext & {
  type: "ended" | "sdk_ended";
};
```

Fields marked optional in the base context are present only when relevant. Treat IDs as opaque and timestamps as browser diagnostics, not security claims.
