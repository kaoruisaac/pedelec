---
title: PedelecSession API
description: PedelecSession 的 properties、turn、events、tool handler、status 與 lifecycle reference。
sidebar:
  order: 2
---

`PedelecSession<TToolName>` 代表一個 Core agent session。

```ts
import type { PedelecSession } from "@kaoruisaac/pedelec";
```

Application 應透過 `Pedelec.createSession()` 或 `Pedelec.resumeSession()` 取得，不直接 constructor。

## Type parameter

```ts
class PedelecSession<TToolName extends string = string>
```

`TToolName` 收窄 named `onTool(name, handler)` overload。`createSession()` 可從 readonly tools array 推導。

## Properties

### `sessionId`

```ts
readonly sessionId: string
```

Core session/thread identifier。使用 `autoEndOnDisconnect: false` 並準備 resume 時保存。

### `provider`

```ts
readonly provider: string
```

此 SDK handle 註冊時已知 provider。Fresh resumed handle 目前可能是 empty string。

### `model`

```ts
readonly model?: string
```

此 SDK handle 已知 model。沒有 explicit/default model，或 fresh resume 後可能是 `undefined`。

### `sessionCreatedAt`

```ts
readonly sessionCreatedAt: number
```

這個 session object construction 的 browser `Date.now()`。Resume 後不保證是原始 Core thread creation time。

## `prepare()`

```ts
prepare(): Promise<void>
```

在第一個 user turn 前 optional prepare provider/session startup。

行為：

- 成功後 idempotent；
- concurrent prepare calls 共用 promise；
- active user turn 時 reject `SESSION_BUSY`；
- ended 後 reject `SESSION_ENDED`；
- preparation chat delta 不送到 `onChat()`；
- 仍可 emit status/error；
- 不是 `sendText()` 必要前置。

```ts
await session.prepare().catch(() => {
  // Preparation 只是 optimization，保留 sendText。
});
```

## `sendText()`

```ts
sendText(text: string): Promise<void>
```

開始一個 user turn。Completion/idle 後 resolve；turn failed、session ended 或已有 active turn 時 reject。

Text output 由 `onChat()` 提供。

```ts
await session.sendText("Analyze the current page.");
```

SDK 不 trim，也不強制 non-empty；composer 應自行 validate。

常見 errors：`SESSION_BUSY`、`SESSION_ENDED`、`SEND_TEXT_FAILED`、`SESSION_ERROR` 與 transport/provider error。

## `uploadAsset()`

```ts
uploadAsset(file: File): Promise<SandboxAssetPath>
uploadAsset(file: File, targetPath: SandboxAssetPath): Promise<SandboxAssetPath>
```

Session 尚未結束時可 upload 一個 browser `File`。upload 可與 prepare、send 和 provider execution 同時進行，但每個 session 只允許一個 upload。單檔上限 100 MiB；成功時回傳以 `assets/` 為隱含根目錄的 `/...` path。傳入 `targetPath` 可精確寫入 nested path，自動建立父目錄並覆蓋既有一般檔案。

內容經由 Desktop 內部的 `127.0.0.1` loopback asset transfer server 傳輸，不經 Extension、Native Messaging 或 Core IPC message body。SDK request、loopback upload `PUT` 與 ticket 都可能失敗，請見 [Error Codes](./error-codes/)。

## `listAssets()`

```ts
listAssets(): Promise<SandboxAsset[]>
```

回傳 App 與 Agent 共用 `assets/` 目錄第一層中已完成的一般檔案，並依 filesystem modification time 由新到舊排序。此 listing 不遞迴，因此不會列出 nested assets。

`name` 是實際 sandbox filename，`path` 是 sandbox-relative path，`modifiedAt` 是 filesystem modification time，不保證等同精確的 write completion timestamp。Agent 執行期間仍可 listing；session 正在結束或已結束時會 reject `SESSION_ENDED`。目前不支援 pagination、delete、rename、move 或 recursive listing。

## `readAsset()`

```ts
readAsset(path: SandboxAssetPath, type: "text"): Promise<string>
readAsset<T = JsonValue>(path: SandboxAssetPath, type: "json"): Promise<T>
readAsset(path: SandboxAssetPath, type: "file"): Promise<File>
```

讀取 shared session `assets/` 中的已知檔案。和 `listAssets()` 不同，這個 method 可接受 `/results/report.json` 之類的 nested public path。Path 必須以 `/` 開頭、使用 `/`，且不能包含空 segment、`.` 或 `..`。

單檔 read 上限為 100 MiB。prepare、send 或 provider execution 進行時仍可讀取；session 正在結束或已結束時會 reject `SESSION_ENDED`。若 Agent 可能仍在寫入相同 path，application 或 agent workflow 應先協調完成時機再讀取。

回傳方式由 `type` 決定：

- `"text"` 使用 strict UTF-8 decode；invalid bytes 會收到 `ASSET_TEXT_DECODE_FAILED`；
- `"json"` 先 strict UTF-8 decode 再 parse JSON；invalid JSON 會收到 `ASSET_INVALID_JSON`。Generic `T` 只提供 compile-time typing，不會驗證 parsed value；
- `"file"` 回傳 browser `File`，並使用 Desktop 回報的 asset name、MIME type 與 modification time。

```ts
const text = await session.readAsset("/report.txt", "text");
const result = await session.readAsset<{ ok: boolean }>(
  "/results/result.json",
  "json",
);
const model = await session.readAsset("/model.glb", "file");
```

File bytes 透過 internal loopback download ticket 傳輸。Application 不應依賴 URL、token、port 或 ticket lifetime。Path、file、size、download、decode 與 parse failure 請見 [Error Codes](./error-codes/)。

## `onChat()`

```ts
onChat(
  handler: (text: string, ctx: ChatEventContext) => void,
): () => void
```

註冊 incremental text handler，回傳 unsubscribe function。

```ts
const off = session.onChat((delta, ctx) => {
  transcript.append(ctx.turnId, delta);
});
```

Preparation output 不送到 chat handlers。

## `onTool()`

### Generic fallback overload

```ts
onTool(
  handler: (
    tool: TToolName,
    args: unknown,
    ctx: ToolCallContext,
  ) => unknown | Promise<unknown>,
): () => void
```

### Named overload

```ts
onTool<TArgs = unknown, TResult = unknown>(
  toolName: TToolName,
  handler: ToolSpecificHandler<TArgs, TResult>,
): () => void
```

Named > inline > generic fallback。

```ts
const off = session.onTool(
  "update_counter",
  (args: { delta: number }) => ({
    value: counter.add(args.delta),
  }),
);
```

Handler 可 sync/async，回傳 JSON-compatible value。Throw 會轉成給 agent 的 `TOOL_HANDLER_ERROR` result。

## `onError()`

```ts
onError(
  handler: (error: PedelecError, ctx: ErrorEventContext) => void,
): () => void
```

觀察 Core 與 SDK session errors。

```ts
const off = session.onError((error, ctx) => {
  console.error(ctx.source, error.code, error.message, error.details);
});
```

仍需 catch `prepare()`、`sendText()`、`end()` promise。

## `onStatus()`

```ts
onStatus(
  handler: (
    status: PedelecSessionStatus,
    ctx: StatusEventContext,
  ) => void,
): () => void
```

Local status value 改變時執行，不會立即 emit initial value。

```ts
renderStatus(session.getStatus());
const off = session.onStatus(renderStatus);
```

## `onEnded()`

```ts
onEnded(handler: (ctx: EndedEventContext) => void): () => void
```

Session 第一次 ended 時執行一次。

```ts
const off = session.onEnded(() => {
  disableComposer();
});
```

## `getStatus()`

```ts
getStatus(): PedelecSessionStatus
```

回傳目前 browser-side status snapshot。

```ts
if (session.getStatus() === "idle") {
  await session.sendText(text);
}
```

適合 UI gating，但不能取代 catch `SESSION_BUSY`，因 check 與 request 之間 state 可能改變。

## `end()`

```ts
end(): Promise<void>
```

結束 Core session 並 mark SDK handle ended。

成功後：

- `onEnded()` 執行一次；
- pending turn/preparation reject `SESSION_ENDED`；
- session 從 client registry 移除；
- 後續 `sendText()`／`prepare()` reject。

呼叫 `end()` 會結束 session，但不會立即刪除其 sandbox。Sandbox 是由 Desktop 管理的暫存資料，不是 application-owned durable storage：Desktop App 正常退出時會清除，異常終止遺留的內容會在下次啟動時嘗試清除。關閉主視窗只會隱藏 App，不會觸發 cleanup。即使 sandbox 暫時仍存在，ended session 仍不能使用 `uploadAsset()`、`listAssets()` 或 `readAsset()`；若強制終止或檔案持續鎖定，cleanup 可能延後到後續啟動。

Already-ended handle 呼叫 `end()` 立即 resolve。

End request 失敗時 emit SDK error 並 reject，不會假裝 Core 已成功 ended。

## Handler cleanup

所有 event registration 都回傳 `() => void` disposer。Owning component/state destroy 時呼叫。

Unsubscribe 不會 cancel 已執行中的 provider turn 或 handler promise。

## Status values

```ts
type PedelecSessionStatus =
  | "idle"
  | "running"
  | "waiting_tool_result"
  | "ended"
  | "error";
```

Transitions 與 context fields 詳見 [Session 狀態與 Events](../sdk/status-and-events/)。
