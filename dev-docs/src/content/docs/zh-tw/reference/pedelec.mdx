---
title: Pedelec API
description: Pedelec browser client 的 constructor 與 method reference。
sidebar:
  order: 1
---

`Pedelec` 是 browser client，擁有 Chrome Extension bridge，並建立或 resume `PedelecSession`。

```ts
import { Pedelec } from "@kaoruisaac/pedelec";
```

## Constructor

```ts
new Pedelec(options?: PedelecOptions)
```

```ts
type PedelecOptions = {
  bridgeTimeoutMs?: number;
};
```

### Parameters

| Parameter | Type | Default | 說明 |
| --- | --- | --- | --- |
| `options.bridgeTimeoutMs` | `number` | `30_000` | 等待 Extension bridge response 的最長時間；小於 1 會 clamp 成 1。 |

Constructor 會立即嘗試連到 Pedelec Chrome Extension。只能在 hydration 後的 browser page 建立。

```ts
const pedelec = new Pedelec({
  bridgeTimeoutMs: 30_000,
});
```

## `createSession()`

```ts
createSession(): Promise<PedelecSession<string>>;

createSession<const TTools extends readonly ToolDefinition[]>(
  input: CreateSessionInput<TTools>,
): Promise<PedelecSession<ToolNameOf<TTools>>>;
```

建立新 Core session，並讓目前 SDK channel subscribe。

### Input

```ts
type CreateSessionInput<TTools extends readonly ToolDefinition[]> =
  | {
      provider: ProviderCode;
      effortLevel?: "default" | "low" | "high";
      skills?: SkillsInput<TTools>;
      sandbox?: CreateSessionSandboxInput;
      autoEndOnDisconnect?: boolean;
    }
  | {
      provider?: undefined;
      effortLevel?: "default" | "low" | "high";
      skills?: SkillsInput<TTools>;
      sandbox?: CreateSessionSandboxInput;
      autoEndOnDisconnect?: boolean;
    };
```

### Resolution rules

- 無 `provider`：使用 Desktop `defaultProvider` 並確認 available。
- `provider` 可搭配 provider-independent `effortLevel`；省略時使用 `default`。
- `provider` + `model`：使用 explicit values。
- `sandbox.path`：可選擇使用 absolute、由 application 管理的 workspace，取代 temporary managed sandbox。
- 只有 `model`：invalid。
- `autoEndOnDisconnect`：預設 `true`。

Explicit `sandbox.path` 可以是新目錄或既有目錄。Core 會建立 Pedelec 擁有的 `.pedelec-sandbox/{skills,assets,logs,tmp}/` runtime 結構，但不清空既有 project 內容。Pedelec 永遠不會刪除 explicit workspace，多個 active session 也可以共用它。Path 不得與 Pedelec managed sandbox root overlap；無效 path 回傳 `SANDBOX_PATH_INVALID`。

### Returns

註冊於此 client 的 `PedelecSession`。

### 常見 errors

- approval/transport errors；
- `DEFAULT_PROVIDER_NOT_SET`；
- `DEFAULT_PROVIDER_UNAVAILABLE`；
- `INVALID_INPUT`；
- Ollama 無 model 的 `MODEL_REQUIRED`；
- provider/Core creation errors。

```ts
const session = await pedelec.createSession({
  provider: "codex",
  autoEndOnDisconnect: true,
});
```

## `sandboxFolderPicker()`

```ts
sandboxFolderPicker(): Promise<SandboxFolderPickerResult | null>
```

透過 Pedelec Desktop 開啟作業系統的原生、僅可選取資料夾的 picker。選取資料夾時回傳 path、根目錄是否為空，以及 `.pedelec-lock.json` 是否為 regular file；關閉或取消時 resolve `null`。

```ts
const folder = await pedelec.sandboxFolderPicker();
if (!folder) return;

if (!folder.isEmptyFolder && !folder.hasSandboxConfig) {
  // application 自行決定是否警告使用者。
}

const session = await pedelec.createSession({
  sandbox: { path: folder.path },
});
```

這是敏感的 Desktop API，可能觸發目前 origin 的 approval flow。Picker 只做 read-only observation，不會驗證或初始化 sandbox、建立 workspace 子目錄、parse marker JSON，也不限制 origin 或 SDK version。`isEmptyFolder` 只反映根目錄當下是否有任何 entry；`hasSandboxConfig` 只判斷 `.pedelec-lock.json` 是否為 regular file，即使內容不是合法 JSON 也算存在。既有的 custom-sandbox validation 仍由 `createSession()` 負責；成功初始化後才建立含 SDK version 與 normalized caller origin 的 `.pedelec-lock.json`。Picker 真正失敗時 reject `DIRECTORY_PICKER_FAILED`；使用者取消不是 error。

## `listProviders()`

```ts
listProviders(): Promise<ProviderInfo[]>
```

取得 Desktop Core 的 provider discovery result。

```ts
type ProviderInfo = {
  name: string;
  code: ProviderCode;
  available: boolean;
  error: string | null;
};
```

```ts
const providers = await pedelec.listProviders();
const ready = providers.filter((provider) => provider.available);
```

`available` 是 discovery/setup signal，不保證所有 model 或 remote service ready。

常見 errors 為 Extension、native host、Core IPC 與 bridge timeout。

## `getSettings()`

```ts
getSettings(): Promise<PedelecSettings>
```

讀取 SDK 可見的 Desktop defaults。

```ts
type PedelecSettings = {
  defaultProvider: ProviderCode | null;
};
```

SDK 會驗證 response shape；不相容時 reject `SDK_PROTOCOL_ERROR`。

```ts
const settings = await pedelec.getSettings();
console.log(settings.defaultProvider);
```

## `getApprovalStatus()`

```ts
getApprovalStatus(): Promise<ApprovalStatus>
```

```ts
type ApprovalStatus = {
  installed: boolean;
  approved: boolean;
  origin: string | null;
  appConnected: boolean;
};
```

讀取目前 origin 的 Extension approval state。

特殊行為：Extension unavailable/disconnected 時 resolve，不 throw：

```ts
{
  installed: false,
  approved: false,
  origin: location.origin,
  appConnected: false
}
```

Protocol/storage 等其他 error 仍可能 reject。

這個 method 不會打開 approval popup；需要時由 `createSession()` 或 `resumeSession()` 觸發。

## `checkAvailability()`

```ts
checkAvailability(): Promise<PedelecAvailability>
```

檢查 Extension、目前 origin 的 approval 與 Desktop readiness；不會建立或 resume session，也不會開啟 approval popup。`getApprovalStatus().appConnected` 是非敏感 Core ping 的連線結果，不代表 approval 或 provider ready。`getSettings()` 與 `listProviders()` 需要 origin approval 並可能開啟 popup，且僅分別公開 defaults 與 `name`/`code`/`available`/`error`。

`launchAttempted: true` 只代表已送出 settings probe，不能證明 Desktop 已啟動；settings response shape 不合法也會被視為 Desktop unavailable。Extension unavailable 也可能只是 bridge 中斷。

## `resumeSession()`

```ts
resumeSession(sessionId: string): Promise<PedelecSession<string>>
```

讓此 SDK client subscribe 到既有 Core session。

```ts
const session = await pedelec.resumeSession(savedSessionId);
```

| Parameter | Type | 說明 |
| --- | --- | --- |
| `sessionId` | `string` | Non-empty Core session/thread identifier。 |

回傳註冊於此 client 的 session handle。目前 bridge 只回傳 ID，因此新的 resumed handle 可能沒有 provider/effort metadata。

常見 errors：空 ID 的 `INVALID_INPUT`、approval errors、caller origin 無法傳遞時的 `SDK_ORIGIN_UNAVAILABLE`、session 屬於其他 origin 時的 `THREAD_ACCESS_DENIED`、Core 已找不到 ID 時的 `THREAD_NOT_FOUND`，以及 transport errors。

Resume 後要重新註冊 chat、status、error、ended 與 tool handlers。

## `request<T>()`

```ts
request<T>(
  type: string,
  payload?: Record<string, unknown>,
): Promise<T>
```

Low-level bridge request API。

```ts
const result = await pedelec.request<{ sessionId: string }>(
  "create_session",
  {
    input: {
      provider: "codex",
      autoEndOnDisconnect: true,
    },
  },
);
```

:::caution
優先使用 typed public methods。`request<T>()` 不驗證 generic `T` 是否符合 runtime response，low-level operation name/payload 也較容易誤用。
:::

此 method：

- lazy 確保目前存在 Extension port，並在暫時 disconnect 後 retry；
- 套用 `bridgeTimeoutMs`；
- 產生 request ID；
- 經 Extension port post；
- 將 failed bridge response normalize 成 `PedelecError`。

`T` 只存在 compile time。

## Client disconnection

Extension port disconnect 時，已透過該 port 送出的 request 會 reject，registered sessions 收到 SDK error event。Parent client 仍可 reuse，下一個新的 operation 會 reconnect；舊 session handle 不會靜默遷移，因此要繼續明確保留的 session 時請呼叫 `resumeSession(sessionId)`。

## Related references

- [`PedelecSession`](./pedelec-session/)
- [Tools API](./tools/)
- [Types](./types/)
- [Error Codes](./error-codes/)
